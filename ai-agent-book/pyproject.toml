[project]
name = "ai-agent-book"
version = "0.1.0"
description = "Course experiments with a standalone LLM client."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "numpy>=2.0.0",
    "openai>=1.0.0",
    "torch>=2.4.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
